MASTER SUPER STORE — RENDER QUICK DEPLOY

Upload this whole folder to GitHub, then:
Render -> New + -> Blueprint -> choose repository -> Apply.

Use the Render live URL, NOT GitHub Pages.

Health check:
<your-render-url>/api/health

If the app opens but Create Account fails, check Render -> Logs and confirm the web service is running.
